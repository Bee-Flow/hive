// Moved to components/shared/SaveStatus so the routines builder's node-config
// modal can use the same chip (BFSF-338). Re-exported here because the IDE
// imports it by this path in several places.
export { default } from '../../components/shared/SaveStatus';
