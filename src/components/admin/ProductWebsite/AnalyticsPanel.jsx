/**
 * Barrel — the dashboard now lives in ./analytics/ (shell + one file per
 * section). Kept at this path so AdminDashboard's import is unchanged.
 */
export { default } from './analytics/AnalyticsPanel';
